<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.kozirase.app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kozirase">
        <activity android:name=".LoginActivity"></activity>

        <activity
            android:name=".AddEditEventActivity"
            android:parentActivityName=".HeartRateProcessActivity" />
        <activity
            android:name=".HeartRateProcessActivity"
            android:launchMode="singleTop" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".ApiConnectionActivity" />
        <activity android:name=".FinalScoreActivity" />
        <activity android:name=".TextInputActivity" />
        <activity android:name=".InterimScoreActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>